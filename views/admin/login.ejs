<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/tailwind.css">
    <style>
        /* Custom Dark Theme Styles */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #212529;
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb {
            background: #a67c52;
            border-radius: 4px;
            border: 1px solid #343a40;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #b8956b;
        }
        .solid-background {
            background: #1a1d20;
        }
        .glow-brown {
            box-shadow: 0 0 20px rgba(166, 124, 82, 0.3);
        }
        .glow-brown:hover {
            box-shadow: 0 0 30px rgba(166, 124, 82, 0.5);
        }
        .pulse-brown {
            animation: pulseBrown 2s infinite;
        }
        @keyframes pulseBrown {
            0% { box-shadow: 0 0 0 0 rgba(166, 124, 82, 0.7); }
            70% { box-shadow: 0 0 0 10px rgba(166, 124, 82, 0); }
            100% { box-shadow: 0 0 0 0 rgba(166, 124, 82, 0); }
        }
        .text-glow {
            text-shadow: 0 0 10px rgba(166, 124, 82, 0.5);
        }
        .float {
            animation: float 6s ease-in-out infinite;
        }
        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
            100% { transform: translateY(0px); }
        }
        .fade-in {
            animation: fadeIn 0.5s ease-in;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .glass-effect {
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            background: rgba(33, 37, 41, 0.8);
            border: 1px solid rgba(166, 124, 82, 0.3);
        }
    </style>
</head>
<body class="bg-dark-900 min-h-screen flex items-center justify-center p-4 solid-background">
    <div class="max-w-md w-full mx-auto fade-in">
        <div class="bg-dark-800 rounded-2xl shadow-2xl p-8 border border-brown-600/30 backdrop-blur-sm glass-effect glow-brown">
            <div class="text-center mb-8">
                <div class="mx-auto w-32 h-32 mb-6 flex items-center justify-center">
                    <img src="/LogoApp.jpeg" alt="Fleur Logo" class="w-32 h-32 object-contain rounded-2xl shadow-lg">
                </div>
                <h1 class="text-3xl font-light text-brown-200 tracking-widest mb-2">FLEUR CAFÉ</h1>
                <p class="text-brown-400 text-base italic font-serif">Atelier d'artistes</p>
                <p class="text-brown-500 mt-3 text-sm">Sistem Presensi - Admin Login</p>
            </div>

            <% if (error && error.length > 0) { %>
                <div class="bg-gradient-to-r from-red-900/80 to-red-800/80 border border-red-600/50 text-red-200 px-4 py-3 rounded-lg mb-4 backdrop-blur-sm">
                    <i class="fas fa-exclamation-circle mr-2"></i>
                    <%= error %>
                </div>
            <% } %>

            <form method="POST" action="/admin/login" class="space-y-6">
                <div>
                    <label for="username" class="block text-sm font-medium text-brown-300 mb-2">
                        <i class="fas fa-user mr-2"></i>Username
                    </label>
                    <input type="text" id="username" name="username" required
                           class="w-full px-4 py-3 bg-dark-700 border border-brown-600/50 rounded-lg focus:ring-2 focus:ring-brown-500 focus:border-brown-400 transition duration-200 text-white placeholder-brown-400">
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium text-brown-300 mb-2">
                        <i class="fas fa-lock mr-2"></i>Password
                    </label>
                    <input type="password" id="password" name="password" required
                           class="w-full px-4 py-3 bg-dark-700 border border-brown-600/50 rounded-lg focus:ring-2 focus:ring-brown-500 focus:border-brown-400 transition duration-200 text-white placeholder-brown-400">
                </div>

                <button type="submit" 
                        class="w-full bg-brown-600 hover:bg-brown-500 text-white font-semibold py-3 px-4 rounded-lg transition duration-200 transform hover:scale-105 shadow-lg hover:shadow-brown-500/30">
                    <i class="fas fa-sign-in-alt mr-2"></i>
                    Login
                </button>
            </form>
        </div>
    </div>
</body>
</html>