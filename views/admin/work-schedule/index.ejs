<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/tailwind.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #212529;
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb {
            background: #a67c52;
            border-radius: 4px;
            border: 1px solid #343a40;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #b8956b;
        }
        .solid-background {
            background: #1a1d20;
        }
    </style>
</head>
<body class="bg-dark-900 min-h-screen solid-background">
    <%
        const q = (filters && filters.q) ? filters.q : '';
        const pageInfo = pagination || { page: 1, totalPages: 1, hasPrev: false, hasNext: false, prevPage: 1, nextPage: 1, startItem: 0, endItem: 0, totalItems: 0 };
        const queryQ = q ? `&q=${encodeURIComponent(q)}` : '';
    %>
    <%- include('../../partials/navbar') %>

    <div class="pt-16 sm:pt-20 lg:pt-20">
        <div class="lg:hidden pt-16 sm:pt-20"></div>
        
        <div class="flex flex-col lg:flex-row">
            <%- include('../../partials/sidebar', { currentPage: 'work-schedule' }) %>

            <div class="flex-1 lg:ml-64 p-4 sm:p-6 lg:p-8 min-h-screen">
                <div class="mb-6 sm:mb-8">
                    <div class="flex items-center justify-between mb-2">
                        <div class="flex items-center space-x-3">
                            <div class="w-8 h-8 flex items-center justify-center">
                                <i class="fas fa-clock text-brown-400 text-xl"></i>
                            </div>
                            <h2 class="text-2xl sm:text-3xl font-bold text-white">Jadwal Kerja</h2>
                        </div>
                        <a href="/admin/work-schedule/add" class="bg-brown-600 hover:bg-brown-500 text-white px-4 sm:px-6 py-2 sm:py-3 rounded-lg transition duration-200 shadow-lg text-sm sm:text-base">
                            <i class="fas fa-plus mr-2"></i>Tambah Jadwal
                        </a>
                    </div>
                    <p class="text-brown-300 text-sm sm:text-base">Kelola jadwal kerja karyawan FLEUR CAFÉ - Atelier d'artistes</p>
                </div>

                <% if (messages.success && messages.success.length > 0) { %>
                    <% messages.success.forEach(msg => { %>
                        <div class="bg-green-900/80 border border-green-600/50 text-green-200 px-4 py-3 rounded-lg mb-4 shadow-lg backdrop-blur-sm">
                            <i class="fas fa-check-circle mr-2"></i><%= msg %>
                        </div>
                    <% }) %>
                <% } %>

                <% if (messages.error && messages.error.length > 0) { %>
                    <% messages.error.forEach(msg => { %>
                        <div class="bg-red-900/80 border border-red-600/50 text-red-200 px-4 py-3 rounded-lg mb-4 shadow-lg backdrop-blur-sm">
                            <i class="fas fa-exclamation-circle mr-2"></i><%= msg %>
                        </div>
                    <% }) %>
                <% } %>

                <form method="GET" action="/admin/work-schedule" class="mb-4 flex flex-col gap-2 sm:flex-row">
                    <input
                        type="text"
                        name="q"
                        value="<%= q %>"
                        placeholder="Cari nama jadwal/shift..."
                        class="w-full rounded-lg border border-brown-700/40 bg-dark-700 px-3 py-2 text-white sm:max-w-sm" />
                    <button type="submit" class="rounded-lg bg-brown-600 px-4 py-2 text-white hover:bg-brown-500">Cari</button>
                    <% if (q) { %>
                        <a href="/admin/work-schedule" class="rounded-lg border border-brown-700/40 px-4 py-2 text-brown-200 hover:bg-dark-700">Reset</a>
                    <% } %>
                </form>

                <div class="bg-dark-800 rounded-xl shadow-2xl overflow-hidden border border-brown-600/30">
                    <!-- Mobile Card View -->
                    <div class="block lg:hidden">
                        <% if (schedules && schedules.length > 0) { %>
                            <% schedules.forEach(schedule => { %>
                                <div class="border-b border-brown-600/30 p-4">
                                    <div class="flex items-start justify-between mb-2">
                                        <h3 class="font-semibold text-brown-300 text-lg"><%= schedule.nama %></h3>
                                        <% if (schedule.is_active) { %>
                                            <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-green-900/50 text-green-300 border border-green-600/50">
                                                <i class="fas fa-check-circle mr-1"></i>Aktif
                                            </span>
                                        <% } else { %>
                                            <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-gray-600 text-white">
                                                <i class="fas fa-times-circle mr-1"></i>Nonaktif
                                            </span>
                                        <% } %>
                                    </div>
                                    <% if (schedule.nama_shift) { %>
                                        <p class="text-sm text-brown-200 mb-2">
                                            <i class="fas fa-briefcase mr-2 text-brown-500"></i>
                                            <%= schedule.nama_shift %>
                                        </p>
                                    <% } %>
                                    <p class="text-sm text-brown-200 mb-2">
                                        <i class="far fa-clock mr-2 text-brown-500"></i>
                                        <%= schedule.jam_masuk.substring(0,5) %> - <%= schedule.jam_keluar.substring(0,5) %>
                                    </p>
                                    <p class="text-sm text-brown-200 mb-2">
                                        <% 
                                        let workDays = [];
                                        try {
                                            workDays = typeof schedule.hari_kerja === 'string' ? JSON.parse(schedule.hari_kerja) : schedule.hari_kerja;
                                        } catch(e) {
                                            workDays = [];
                                        }
                                        const dayMap = {
                                            'Monday': 'Sen', 'Tuesday': 'Sel', 'Wednesday': 'Rab',
                                            'Thursday': 'Kam', 'Friday': 'Jum', 'Saturday': 'Sab', 'Sunday': 'Min'
                                        };
                                        const displayDays = workDays.map(day => dayMap[day] || day).join(', ');
                                        %>
                                        <i class="far fa-calendar mr-2 text-brown-500"></i>
                                        <%= displayDays %>
                                    </p>
                                    <div class="flex items-center justify-between mt-3">
                                        <span class="bg-brown-600 text-white px-3 py-1 rounded-full text-sm font-medium">
                                            <%= schedule.employee_count %> karyawan
                                        </span>
                                        <div class="flex space-x-2">
                                            <a href="/admin/work-schedule/show/<%= schedule.id %>" class="bg-blue-600 hover:bg-blue-700 text-white px-3 py-1 rounded-lg text-sm transition duration-200">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                            <a href="/admin/work-schedule/edit/<%= schedule.id %>" class="bg-yellow-600 hover:bg-yellow-700 text-white px-3 py-1 rounded-lg text-sm transition duration-200">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <% if (schedule.employee_count == 0) { %>
                                                <form action="/admin/work-schedule/delete/<%= schedule.id %>" method="POST" style="display:inline;" class="js-confirm-delete" data-label="jadwal kerja &quot;<%= schedule.nama %>&quot;">
                                                    <button type="submit" class="bg-red-600 hover:bg-red-700 text-white px-3 py-1 rounded-lg text-sm transition duration-200">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </form>
                                            <% } %>
                                        </div>
                                    </div>
                                </div>
                            <% }) %>
                        <% } else { %>
                            <div class="p-6 text-center text-brown-300">
                                <i class="fas fa-calendar-times text-4xl mb-4 text-brown-400"></i>
                                <p>Belum ada jadwal kerja</p>
                            </div>
                        <% } %>
                    </div>

                    <!-- Desktop Table View -->
                    <div class="hidden lg:block overflow-x-auto">
                        <table class="min-w-full divide-y divide-brown-600/30">
                            <thead class="bg-brown-800">
                                <tr>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-brown-200 uppercase tracking-wider">Nama Jadwal</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-brown-200 uppercase tracking-wider">Shift</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-brown-200 uppercase tracking-wider">Jam Kerja</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-brown-200 uppercase tracking-wider">Hari Kerja</th>
                                    <th class="px-6 py-3 text-center text-xs font-medium text-brown-200 uppercase tracking-wider">Karyawan</th>
                                    <th class="px-6 py-3 text-center text-xs font-medium text-brown-200 uppercase tracking-wider">Status</th>
                                    <th class="px-6 py-3 text-center text-xs font-medium text-brown-200 uppercase tracking-wider">Aksi</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-brown-600/30">
                                <% if (schedules && schedules.length > 0) { %>
                                    <% schedules.forEach(schedule => { %>
                                        <tr class="hover:bg-dark-600/50 transition duration-200">
                                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-white">
                                                <%= schedule.nama %>
                                            </td>
                                            <td class="px-6 py-4 whitespace-nowrap text-sm text-brown-200">
                                                <% if (schedule.nama_shift) { %>
                                                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-brown-900/50 text-brown-300 border border-brown-600/50">
                                                        <i class="fas fa-briefcase mr-1"></i><%= schedule.nama_shift %>
                                                    </span>
                                                <% } else { %>
                                                    <span class="text-brown-500">-</span>
                                                <% } %>
                                            </td>
                                            <td class="px-6 py-4 whitespace-nowrap text-sm text-brown-200">
                                                <i class="far fa-clock mr-2 text-brown-500"></i>
                                                <%= schedule.jam_masuk.substring(0,5) %> - <%= schedule.jam_keluar.substring(0,5) %>
                                            </td>
                                            <td class="px-6 py-4 whitespace-nowrap text-sm text-brown-200">
                                                <% 
                                                let workDays = [];
                                                try {
                                                    workDays = typeof schedule.hari_kerja === 'string' ? JSON.parse(schedule.hari_kerja) : schedule.hari_kerja;
                                                } catch(e) {
                                                    workDays = [];
                                                }
                                                const dayMap = {
                                                    'Monday': 'Sen', 'Tuesday': 'Sel', 'Wednesday': 'Rab',
                                                    'Thursday': 'Kam', 'Friday': 'Jum', 'Saturday': 'Sab', 'Sunday': 'Min'
                                                };
                                                const displayDays = workDays.map(day => dayMap[day] || day).join(', ');
                                                %>
                                                <i class="far fa-calendar mr-2 text-brown-500"></i>
                                                <%= displayDays %>
                                            </td>
                                            <td class="px-6 py-4 whitespace-nowrap text-center">
                                                <span class="inline-flex px-3 py-1 text-xs font-semibold rounded-full bg-brown-900/50 text-brown-300 border border-brown-600/50">
                                                    <%= schedule.employee_count %> orang
                                                </span>
                                            </td>
                                            <td class="px-6 py-4 whitespace-nowrap text-center">
                                                <% if (schedule.is_active) { %>
                                                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-green-900/50 text-green-300 border border-green-600/50">
                                                        <i class="fas fa-check-circle mr-1"></i>Aktif
                                                    </span>
                                                <% } else { %>
                                                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-gray-900/50 text-gray-300 border border-gray-600/50">
                                                        <i class="fas fa-times-circle mr-1"></i>Nonaktif
                                                    </span>
                                                <% } %>
                                            </td>
                                            <td class="px-6 py-4 whitespace-nowrap text-center text-sm font-medium">
                                                <a href="/admin/work-schedule/show/<%= schedule.id %>" class="text-blue-400 hover:text-blue-300 transition duration-200 mr-3">
                                                    <i class="fas fa-eye mr-1"></i>Detail
                                                </a>
                                                <a href="/admin/work-schedule/edit/<%= schedule.id %>" class="text-yellow-400 hover:text-yellow-300 transition duration-200 mr-3">
                                                    <i class="fas fa-edit mr-1"></i>Edit
                                                </a>
                                                <% if (schedule.employee_count == 0) { %>
                                                    <form action="/admin/work-schedule/delete/<%= schedule.id %>" method="POST" style="display:inline;" class="js-confirm-delete" data-label="jadwal kerja &quot;<%= schedule.nama %>&quot;">
                                                        <button type="submit" class="text-red-400 hover:text-red-300 transition duration-200">
                                                            <i class="fas fa-trash mr-1"></i>Hapus
                                                        </button>
                                                    </form>
                                                <% } %>
                                            </td>
                                        </tr>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="7" class="px-6 py-8 text-center text-brown-300">
                                            <i class="fas fa-calendar-times text-4xl mb-4 text-brown-400"></i>
                                            <p>Belum ada jadwal kerja</p>
                                        </td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>

                <div class="mt-4 flex flex-col gap-2 text-sm text-brown-300 sm:flex-row sm:items-center sm:justify-between">
                    <div>Menampilkan <%= pageInfo.startItem %>-<%= pageInfo.endItem %> dari <%= pageInfo.totalItems %> data</div>
                    <div class="flex items-center gap-2">
                        <a href="/admin/work-schedule?page=<%= pageInfo.prevPage %><%= queryQ %>" class="rounded border border-brown-700/40 px-3 py-1 <%= pageInfo.hasPrev ? 'text-brown-200 hover:bg-dark-700' : 'pointer-events-none opacity-40 text-brown-500' %>">Prev</a>
                        <span class="text-brown-200">Hal <%= pageInfo.page %>/<%= pageInfo.totalPages %></span>
                        <a href="/admin/work-schedule?page=<%= pageInfo.nextPage %><%= queryQ %>" class="rounded border border-brown-700/40 px-3 py-1 <%= pageInfo.hasNext ? 'text-brown-200 hover:bg-dark-700' : 'pointer-events-none opacity-40 text-brown-500' %>">Next</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Real-time Clock
        function updateClock() {
            try {
                const now = new Date();
                const options = {
                    day: '2-digit',
                    month: '2-digit', 
                    year: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit',
                    second: '2-digit',
                    timeZone: 'Asia/Makassar' // WITA timezone
                };
                
                const timeString = now.toLocaleString('id-ID', options);
                const clockElement = document.getElementById('currentTime');
                
                if (clockElement) {
                    clockElement.textContent = timeString;
                }
            } catch (error) {
                console.error('Clock update error:', error);
            }
        }

        document.addEventListener('DOMContentLoaded', function() {
            const clockElement = document.getElementById('currentTime');

            if (clockElement) {
                updateClock();
                setInterval(updateClock, 1000);
            }
        });

        document.addEventListener('submit', async function (event) {
            const form = event.target.closest('form.js-confirm-delete');
            if (!form) return;
            event.preventDefault();
            const label = form.dataset.label || 'jadwal ini';
            const ok = await Notify.confirm({
                title: `Hapus ${label}?`,
                message: 'Tindakan ini tidak bisa dibatalkan.',
                okText: 'Ya, Hapus',
                cancelText: 'Batal',
                variant: 'danger',
                icon: 'danger'
            });
            if (ok) form.submit();
        });
    </script>

    <%- include('../../partials/notify') %>
</body>
</html>
